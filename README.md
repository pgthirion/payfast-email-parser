# payfast-email-parser
A Python automation script that fetches unread PayFast order emails via IMAP, parses the order details (customer, products, discounts), and exports them to a formatted Excel spreadsheet.
